apiVersion: v1
kind: Service
metadata: 
   name: jenkins-service
   labels:
      name: jenkins-service
spec:
  type: ClusterIP
  selector:
     name: jenkins
     app: jenkins-ci
  ports:
   - port: 8081
     targetPort: 8080
