apiVesion: v1
kind: Service
metadata: 
   name: jenkins-service
   labels:
      name: jenkins-service
spec:
  selector:
     name: jenkins
     app: jenkins-ci
  ports:
   - port: 8081
     nodePort: 30001
     targetPort: 8080
